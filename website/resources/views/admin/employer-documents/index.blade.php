<x-admin-layout title="Employer Documents | Luckyboss Admin" heading="Employer Documents">
    <p style="color:#667085">Store and review employer verification documents.</p>@if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    <form method="POST" action="{{ route('admin.employer-documents.store') }}" enctype="multipart/form-data" class="admin-card" style="display:grid;grid-template-columns:1fr 1.5fr 1fr 1.5fr auto;gap:12px;padding:16px;margin:18px 0">@csrf<select class="admin-input" style="margin:0" name="company_id" required><option value="">Company</option>@foreach($companies as $company)<option value="{{ $company->id }}">{{ $company->name }}</option>@endforeach</select><input class="admin-input" style="margin:0" name="name" placeholder="Document name" required><select class="admin-input" style="margin:0" name="status"><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select><input class="admin-input" style="margin:0" type="file" name="file"><button class="admin-button">Add</button></form>
    <form method="GET" class="admin-card" style="display:flex;gap:12px;padding:16px;margin-bottom:18px"><select class="admin-input" style="margin:0;max-width:300px" name="company_id"><option value="">All companies</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected((string)($filters['company_id'] ?? '') === (string)$company->id)>{{ $company->name }}</option>@endforeach</select><select class="admin-input" style="margin:0;max-width:220px" name="status"><option value="">All statuses</option>@foreach(['pending','approved','rejected'] as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '') === $status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="admin-button">Filter</button></form>
    <section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:800px"><thead><tr><th>Company</th><th>Document</th><th>File</th><th>Status</th><th>Reviewed</th><th style="text-align:right">Actions</th></tr></thead><tbody>@forelse($documents as $document)<tr><td>{{ $document->company->name }}</td><td>{{ $document->name }}</td><td>{{ $document->file_path ? basename($document->file_path) : 'No file' }}</td><td><span class="admin-badge">{{ str($document->status)->headline() }}</span></td><td>{{ $document->reviewed_at?->format('d M Y H:i') ?: '-' }}</td><td style="text-align:right"><form method="POST" action="{{ route('admin.employer-documents.update', $document) }}" style="display:inline">@csrf @method('PUT')<input type="hidden" name="name" value="{{ $document->name }}"><select name="status" onchange="this.form.submit()"><option value="pending" @selected($document->status === 'pending')>Pending</option><option value="approved" @selected($document->status === 'approved')>Approved</option><option value="rejected" @selected($document->status === 'rejected')>Rejected</option></select></form><form method="POST" action="{{ route('admin.employer-documents.destroy', $document) }}" style="display:inline">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318;cursor:pointer;margin-left:10px">Delete</button></form></td></tr>@empty<tr><td colspan="6" style="padding:24px;color:#667085">No employer documents found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $documents->links() }}</div>
</x-admin-layout>
