<x-admin-layout title="{{ $definition['label'] }} | Luckyboss Admin" heading="{{ $definition['label'] }}"><div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:22px"><div><h2 style="margin:0">{{ $definition['label'] }}</h2><p style="margin:5px 0;color:#667085">Configure data used across website, portals, and APIs.</p></div><a class="admin-button" href="{{ route('admin.masters.create', $master) }}"><i data-lucide="plus" style="width:16px;margin-right:6px"></i>Add record</a></div>@if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif<section class="admin-card" style="padding:8px 18px"><table class="admin-table"><thead><tr><th>Order</th><th>Icon</th><th>Name</th><th>Slug</th><th>Status</th><th style="text-align:right">Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->sort_order ?? 0 }}</td><td>@if($record->icon_image_path)<img src="{{ asset($record->icon_image_path) }}" alt="{{ $record->name }}" style="width:36px;height:36px;object-fit:contain">@else{{ $record->icon ?: '-' }}@endif</td><td><strong>{{ $record->name }}</strong></td><td style="color:#667085">{{ $record->slug }}</td><td><span class="admin-badge" style="background:{{ $record->is_active ? '#ecfdf3' : '#f2f4f7' }};color:{{ $record->is_active ? '#027a48' : '#667085' }}">{{ $record->is_active ? 'Active' : 'Inactive' }}</span></td><td style="text-align:right"><a href="{{ route('admin.masters.edit', [$master, $record->id]) }}" style="color:#465fff;font-weight:700">Edit</a><form method="POST" action="{{ route('admin.masters.destroy', [$master, $record->id]) }}" style="display:inline">@csrf @method('DELETE')<button type="submit" style="border:0;background:none;color:#b42318;cursor:pointer;margin-left:10px">Delete</button></form></td></tr>@empty<tr><td colspan="6" style="padding:20px;color:#667085">No records found.</td></tr>@endforelse</tbody></table></section></x-admin-layout>