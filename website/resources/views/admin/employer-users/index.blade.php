<x-admin-layout title="Employer Users | Luckyboss Admin" heading="Employer Users">
    <p style="color:#667085">Review employer accounts and enable or disable their company access.</p>
    @if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    <form method="GET" class="admin-card" style="display:grid;grid-template-columns:2fr 1fr auto;gap:12px;padding:16px;margin:18px 0"><input class="admin-input" style="margin:0" name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search user name or email"><select class="admin-input" style="margin:0" name="company_id"><option value="">All companies</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected((string)($filters['company_id'] ?? '') === (string)$company->id)>{{ $company->name }}</option>@endforeach</select><button class="admin-button">Filter</button></form>
    <section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:700px"><thead><tr><th>User</th><th>Company</th><th>Role</th><th>Access</th><th style="text-align:right">Action</th></tr></thead><tbody>@forelse($users as $user)@foreach($user->companies as $company)<tr><td><strong>{{ $user->name }}</strong><br>{{ $user->email }}</td><td>{{ $company->name }}</td><td>{{ str($company->pivot->company_role)->headline() }}</td><td><span class="admin-badge" style="background:{{ $company->pivot->is_active ? '#ecfdf3' : '#f2f4f7' }};color:{{ $company->pivot->is_active ? '#027a48' : '#667085' }}">{{ $company->pivot->is_active ? 'Enabled' : 'Disabled' }}</span></td><td style="text-align:right"><form method="POST" action="{{ route('admin.employer-users.toggle', [$user, $company]) }}">@csrf<button class="admin-button" style="min-height:32px;background:{{ $company->pivot->is_active ? '#b42318' : '#027a48' }}">{{ $company->pivot->is_active ? 'Disable' : 'Enable' }}</button></form></td></tr>@endforeach @empty<tr><td colspan="5" style="padding:24px;color:#667085">No employer users found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $users->links() }}</div>
</x-admin-layout>
