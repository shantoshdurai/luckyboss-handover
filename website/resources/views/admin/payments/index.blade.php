<x-admin-layout title="{{ str($view)->headline() }} | Luckyboss Admin" heading="{{ str($view)->headline() }}">
    <p style="color:#667085">Manage payment transactions, gateway records, webhook payloads, logs, refunds, and invoices.</p>@if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    @if($view === 'payment-gateways')<section class="admin-card" style="padding:8px 18px"><table class="admin-table"><thead><tr><th>Gateway</th><th>Provider</th><th>Environment</th><th>Enabled</th><th>Usage</th></tr></thead><tbody>@forelse($gateways as $gateway)<tr><td>{{ $gateway->name }}</td><td>{{ $gateway->provider ?: '-' }}</td><td>{{ str($gateway->environment)->headline() }}</td><td>{{ $gateway->is_enabled ? 'Enabled' : 'Disabled' }}</td><td>{{ number_format($gateway->usage_count) }}</td></tr>@empty<tr><td colspan="5" style="padding:24px;color:#667085">No payment gateways configured.</td></tr>@endforelse</tbody></table></section>
    @elseif($view === 'invoices')<section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table"><thead><tr><th>Invoice</th><th>Type</th><th>Currency</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td>{{ $invoice->number }}</td><td>{{ str($invoice->type)->headline() }}</td><td>{{ $invoice->currency_code }}</td><td>{{ number_format($invoice->amount, 2) }}</td><td><form method="POST" action="{{ route('admin.invoices.update', $invoice) }}">@csrf @method('PUT')<select name="status" onchange="this.form.submit()"><option value="draft" @selected($invoice->status === 'draft')>Draft</option><option value="issued" @selected($invoice->status === 'issued')>Issued</option><option value="paid" @selected($invoice->status === 'paid')>Paid</option><option value="void" @selected($invoice->status === 'void')>Void</option><option value="refunded" @selected($invoice->status === 'refunded')>Refunded</option></select></form></td><td><form method="POST" action="{{ route('admin.invoices.destroy', $invoice) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="6" style="padding:24px;color:#667085">No invoices found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $invoices->links() }}</div>
    @else<section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:1100px"><thead><tr><th>Reference</th><th>Account</th><th>Purpose</th><th>Gateway</th><th>Amount</th><th>Status</th><th>Payload</th><th>Actions</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->reference }}</td><td>{{ $payment->company?->name ?: $payment->user?->name ?: '-' }}</td><td>{{ str($payment->purpose)->headline() }}</td><td>{{ $payment->gateway }}</td><td>{{ $payment->currency_code }} {{ number_format($payment->amount, 2) }}</td><td><form method="POST" action="{{ route('admin.payments.update', $payment) }}">@csrf @method('PUT')<select name="status" onchange="this.form.submit()"><option value="pending" @selected($payment->status === 'pending')>Pending</option><option value="paid" @selected($payment->status === 'paid')>Paid</option><option value="failed" @selected($payment->status === 'failed')>Failed</option><option value="refunded" @selected($payment->status === 'refunded')>Refunded</option></select></form></td><td>{{ $payment->gateway_payload ? 'Recorded' : '-' }}</td><td><form method="POST" action="{{ route('admin.payments.destroy', $payment) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="8" style="padding:24px;color:#667085">No payment records found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $payments->links() }}</div>@endif
</x-admin-layout>
