<x-admin-layout title="{{ str($view)->headline() }} | Luckyboss Admin" heading="{{ str($view)->headline() }}">
    <p style="color:#667085">Manage reusable templates and communication delivery history.</p>@if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    @if(in_array($view, ['email-templates','whatsapp-templates','interview-templates','offer-templates','rejection-templates','joining-templates'], true))<form method="POST" action="{{ route('admin.communication.templates.store') }}" class="admin-card" style="display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:12px;padding:16px;margin:18px 0">@csrf<input type="hidden" name="type" value="{{ str($view)->before('-templates') }}"><input class="admin-input" style="margin:0" name="name" placeholder="Template name" required><input class="admin-input" style="margin:0" name="subject" placeholder="Subject"><button class="admin-button">Add template</button><textarea class="admin-input" style="grid-column:1/-1;margin:0" name="body" placeholder="Template body with {name}, {job_title}, and other placeholders" rows="4" required></textarea></form><section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:900px"><thead><tr><th>Name</th><th>Subject</th><th>Body</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($templates as $template)<tr><td>{{ $template->name }}</td><td>{{ $template->subject ?: '-' }}</td><td>{{ Str::limit($template->body, 100) }}</td><td><form method="POST" action="{{ route('admin.communication.templates.update', $template) }}">@csrf @method('PUT')<input type="hidden" name="type" value="{{ $template->type }}"><input type="hidden" name="name" value="{{ $template->name }}"><input type="hidden" name="body" value="{{ $template->body }}"><input type="hidden" name="subject" value="{{ $template->subject }}"><input type="hidden" name="is_active" value="{{ $template->is_active ? 0 : 1 }}"><button style="border:0;background:none;color:#465fff">{{ $template->is_active ? 'Active' : 'Inactive' }}</button></form></td><td><form method="POST" action="{{ route('admin.communication.templates.destroy', $template) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="5" style="padding:24px;color:#667085">No templates found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $templates->links() }}</div>
    @else @php($filteredLogs = $view === 'scheduled-messages' ? $logs->where('status', 'queued') : $logs)<section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:950px"><thead><tr><th>Channel</th><th>Sender</th><th>Recipient</th><th>Subject</th><th>Status</th><th>Sent</th><th>Error</th><th>Action</th></tr></thead><tbody>@forelse($filteredLogs as $log)<tr><td>{{ str($log->channel)->headline() }}</td><td>{{ $log->sender?->name ?: 'System' }}</td><td>{{ $log->recipient?->name ?: '-' }}</td><td>{{ $log->subject ?: '-' }}</td><td>{{ str($log->status)->headline() }}</td><td>{{ $log->sent_at?->format('d M Y H:i') ?: '-' }}</td><td>{{ $log->error ?: '-' }}</td><td><form method="POST" action="{{ route('admin.communication.logs.destroy', $log) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="8" style="padding:24px;color:#667085">No communication logs found.</td></tr>@endforelse</tbody></table></section>@endif
</x-admin-layout>
