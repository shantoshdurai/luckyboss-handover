<x-admin-layout title="{{ str($module)->headline() }} | Luckyboss Admin" heading="{{ str($module)->headline() }}"><div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:22px"><div><h2 style="margin:0">{{ str($module)->headline() }}</h2><p style="color:#667085">Reusable central configuration records.</p></div><a class="admin-button" href="{{ route('admin.records.create',$module) }}">Add record</a></div>@if(session('success'))<p style="color:#027a48">{{ session('success') }}</p>@endif<section class="admin-card" style="padding:12px 18px"><table class="admin-table"><thead><tr><th>Order</th><th>Icon</th><th>Name</th><th>Description</th><th>Status</th><th style="text-align:right">Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ data_get($record->payload,'sort_order',0) }}</td><td>@if(data_get($record->payload,'icon_image_url'))<img src="{{ data_get($record->payload,'icon_image_url') }}" alt="{{ $record->name }}" style="width:36px;height:36px;object-fit:contain">@else-@endif</td><td><strong>{{ $record->name }}</strong></td><td>{{ $record->description }}</td><td>{{ $record->is_active?'Active':'Inactive' }}</td><td style="text-align:right"><a style="color:#465fff" href="{{ route('admin.records.edit',[$module,$record]) }}">Edit</a><form style="display:inline" method="POST" action="{{ route('admin.records.destroy',[$module,$record]) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="6">No records.</td></tr>@endforelse</tbody></table></section></x-admin-layout>