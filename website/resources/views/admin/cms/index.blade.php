<x-admin-layout title="{{ str($view)->headline() }} | Luckyboss Admin" heading="{{ str($view)->headline() }}">
    <p style="color:#667085">Manage public website content, featured inventory, and reusable CMS records.</p>@if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    @if($view === 'featured-jobs')<section class="admin-card" style="padding:8px 18px"><table class="admin-table"><thead><tr><th>Job</th><th>Company</th><th>Status</th><th>Featured</th><th>Closing</th></tr></thead><tbody>@forelse($featuredJobs as $job)<tr><td>{{ $job->title }}</td><td>{{ $job->company->name }}</td><td>{{ str($job->status)->headline() }}</td><td>{{ $job->is_featured ? 'Yes' : 'No' }}</td><td>{{ $job->closing_date?->format('d M Y') ?: 'Open' }}</td></tr>@empty<tr><td colspan="5" style="padding:24px;color:#667085">No featured jobs found.</td></tr>@endforelse</tbody></table></section>
    @elseif($view === 'featured-employers')<section class="admin-card" style="padding:8px 18px"><table class="admin-table"><thead><tr><th>Company</th><th>Industry</th><th>Country</th><th>Status</th></tr></thead><tbody>@forelse($featuredEmployers as $company)<tr><td>{{ $company->name }}</td><td>{{ $company->industry ?: '-' }}</td><td>{{ $company->country_code ?: '-' }}</td><td>{{ str($company->status)->headline() }}</td></tr>@empty<tr><td colspan="4" style="padding:24px;color:#667085">No verified employers found.</td></tr>@endforelse</tbody></table></section>
    @else<form method="POST" action="{{ route('admin.cms.records.store') }}" class="admin-card" style="display:grid;grid-template-columns:1fr 2fr auto;gap:12px;padding:16px;margin:18px 0">@csrf<input type="hidden" name="view" value="{{ $view }}"><input class="admin-input" style="margin:0" name="name" placeholder="Record name" required><input class="admin-input" style="margin:0" name="description" placeholder="Description"><button class="admin-button">Add record</button><textarea class="admin-input" style="grid-column:1/-1;margin:0" name="payload" placeholder='JSON payload for structured CMS data' rows="4"></textarea><label><input type="checkbox" name="is_active" value="1" checked> Active</label></form><section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table"><thead><tr><th>Name</th><th>Description</th><th>Payload</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->name }}</td><td>{{ $record->description ?: '-' }}</td><td>{{ json_encode($record->payload) }}</td><td>{{ $record->is_active ? 'Active' : 'Inactive' }}</td><td><form method="POST" action="{{ route('admin.cms.records.destroy', $record) }}">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318">Delete</button></form></td></tr>@empty<tr><td colspan="5" style="padding:24px;color:#667085">No CMS records found.</td></tr>@endforelse</tbody></table></section>@endif
</x-admin-layout>
