<x-admin-layout title="{{ str($view)->headline() }} | Luckyboss Admin" heading="{{ str($view)->headline() }}">
    <p style="color:#667085">Live operational report for the selected platform area.</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px;margin:18px 0">@foreach($metrics as $label=>$value)<article class="admin-card" style="padding:20px"><span style="color:#667085">{{ $label }}</span><strong style="display:block;margin-top:8px;font-size:27px">{{ is_numeric($value) ? number_format($value, 2) : $value }}</strong></article>@endforeach</div><section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table"><thead><tr><th>Record</th><th>Details</th><th>Status / Total</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row->name ?? $row->title ?? $row->reference ?? $row->number ?? $row->source ?? $row->country_code ?? $row->code ?? ('#'.$row->id) }}</td><td>{{ $row->email ?? $row->company?->name ?? $row->description ?? $row->provider ?? $row->data_type ?? $row->category ?? '-' }}</td><td>{{ $row->status ?? $row->total ?? $row->jobs_count ?? $row->usage_count ?? '-' }}</td></tr>@empty<tr><td colspan="3" style="padding:24px;color:#667085">No report records found.</td></tr>@endforelse</tbody></table></section>
</x-admin-layout>
