<x-admin-layout title="Employer Activity | Luckyboss Admin" heading="Employer Activity">
    <p style="color:#667085">Company-scoped activity from the platform audit log.</p>
    <form method="GET" class="admin-card" style="display:flex;gap:12px;padding:16px;margin:18px 0"><select class="admin-input" style="margin:0;max-width:360px" name="company_id"><option value="">All companies</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected((string)($filters['company_id'] ?? '') === (string)$company->id)>{{ $company->name }}</option>@endforeach</select><button class="admin-button">Filter</button></form>
    <section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:800px"><thead><tr><th>Time</th><th>Company</th><th>Actor</th><th>Action</th><th>Entity</th><th>IP</th></tr></thead><tbody>@forelse($activities as $activity)<tr><td>{{ $activity->created_at->format('d M Y H:i') }}</td><td>{{ $activity->company?->name ?: 'Unknown' }}</td><td>{{ $activity->user?->name ?: 'System' }}</td><td>{{ str($activity->action)->headline() }}</td><td>{{ $activity->entity_type ? str($activity->entity_type)->afterLast('\\')->headline().' #'.$activity->entity_id : '-' }}</td><td>{{ $activity->ip_address ?: '-' }}</td></tr>@empty<tr><td colspan="6" style="padding:24px;color:#667085">No employer activity recorded.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $activities->links() }}</div>
</x-admin-layout>
