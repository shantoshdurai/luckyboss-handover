<x-admin-layout title="{{ $note ? 'Edit' : 'Add' }} Employer Note" heading="Employer Notes">
    <div style="max-width:760px"><a href="{{ route('admin.employer-notes.index') }}" style="color:#465fff;font-weight:700">Back to Employer Notes</a><section class="admin-card" style="padding:26px;margin-top:16px"><h2 style="margin-top:0">{{ $note ? 'Edit employer note' : 'Add employer note' }}</h2><form method="POST" action="{{ $note ? route('admin.employer-notes.update', $note) : route('admin.employer-notes.store') }}" style="display:grid;gap:16px">@csrf @if($note) @method('PUT') @endif<label>Company<select class="admin-input" name="company_id" required><option value="">Select company</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected((string)old('company_id', $note?->company_id) === (string)$company->id)>{{ $company->name }}</option>@endforeach</select></label><label>Internal note<textarea class="admin-input" name="note" rows="8" required>{{ old('note', $note?->note) }}</textarea></label>@if($errors->any())<p style="color:#b42318">{{ $errors->first() }}</p>@endif<button class="admin-button">Save note</button></form></section></div>
</x-admin-layout>
