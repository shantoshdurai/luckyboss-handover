<x-admin-layout title="Employer Notes | Luckyboss Admin" heading="Employer Notes">
    <div style="display:flex;justify-content:space-between;align-items:end;gap:16px;margin-bottom:22px"><div><p style="margin:0;color:#667085">Internal notes are visible only to authorized administrators.</p><h2 style="margin:6px 0 0">Employer notes</h2></div><a class="admin-button" href="{{ route('admin.employer-notes.create') }}">Add note</a></div>
    @if(session('success'))<p style="color:#027a48;font-weight:700">{{ session('success') }}</p>@endif
    <form method="GET" class="admin-card" style="display:grid;grid-template-columns:1fr 2fr auto;gap:12px;padding:16px;margin-bottom:18px"><select class="admin-input" style="margin:0" name="company_id"><option value="">All companies</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected((string)($filters['company_id'] ?? '') === (string)$company->id)>{{ $company->name }}</option>@endforeach</select><input class="admin-input" style="margin:0" name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search note text"><button class="admin-button">Filter</button></form>
    <section class="admin-card" style="padding:8px 18px;overflow:auto"><table class="admin-table" style="min-width:700px"><thead><tr><th>Company</th><th>Note</th><th>Author</th><th>Updated</th><th style="text-align:right">Actions</th></tr></thead><tbody>@forelse($notes as $note)<tr><td><strong>{{ $note->company->name }}</strong></td><td style="white-space:pre-line">{{ $note->note }}</td><td>{{ $note->author?->name ?: 'System' }}</td><td>{{ $note->updated_at->format('d M Y H:i') }}</td><td style="text-align:right;white-space:nowrap"><a href="{{ route('admin.employer-notes.edit', $note) }}" style="color:#465fff;font-weight:700">Edit</a><form method="POST" action="{{ route('admin.employer-notes.destroy', $note) }}" style="display:inline">@csrf @method('DELETE')<button style="border:0;background:none;color:#b42318;cursor:pointer;margin-left:10px">Delete</button></form></td></tr>@empty<tr><td colspan="5" style="padding:24px;color:#667085">No employer notes found.</td></tr>@endforelse</tbody></table></section><div style="margin-top:18px">{{ $notes->links() }}</div>
</x-admin-layout>
